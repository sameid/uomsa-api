/**
 * Object#toString() ref for stringify().
 */

var toString = Object.prototype.toString;

/**
 * Object#hasOwnProperty ref
 */

var hasOwnProperty = Object.prototype.hasOwnProperty;

/**
 * Array#indexOf shim.
 */

var indexOf = typeof Array.prototype.indexOf === 'function'
  ? function(arr, el) { return arr.indexOf(el); ***REMOVED***
  : function(arr, el) {
      for (var i = 0; i < arr.length; i++) {
        if (arr[i] === el) return i;
      ***REMOVED***
      return -1;
    ***REMOVED***;

/**
 * Array.isArray shim.
 */

var isArray = Array.isArray || function(arr) {
  return toString.call(arr) == '[object Array]';
***REMOVED***;

/**
 * Object.keys shim.
 */

var objectKeys = Object.keys || function(obj) {
  var ret = [];
  for (var key in obj) {
    if (obj.hasOwnProperty(key)) {
      ret.push(key);
    ***REMOVED***
  ***REMOVED***
  return ret;
***REMOVED***;

/**
 * Array#forEach shim.
 */

var forEach = typeof Array.prototype.forEach === 'function'
  ? function(arr, fn) { return arr.forEach(fn); ***REMOVED***
  : function(arr, fn) {
      for (var i = 0; i < arr.length; i++) fn(arr[i]);
    ***REMOVED***;

/**
 * Array#reduce shim.
 */

var reduce = function(arr, fn, initial) {
  if (typeof arr.reduce === 'function') return arr.reduce(fn, initial);
  var res = initial;
  for (var i = 0; i < arr.length; i++) res = fn(res, arr[i]);
  return res;
***REMOVED***;

/**
 * Cache non-integer test regexp.
 */

var isint = /^[0-9]+$/;

function promote(parent, key) {
  if (parent[key].length == 0) return parent[key] = {***REMOVED***
  var t = {***REMOVED***;
  for (var i in parent[key]) {
    if (hasOwnProperty.call(parent[key], i)) {
      t[i] = parent[key][i];
    ***REMOVED***
  ***REMOVED***
  parent[key] = t;
  return t;
***REMOVED***

function parse(parts, parent, key, val) {
  var part = parts.shift();
  
  // illegal
  if (Object.getOwnPropertyDescriptor(Object.prototype, key)) return;
  
  // end
  if (!part) {
    if (isArray(parent[key])) {
      parent[key].push(val);
    ***REMOVED*** else if ('object' == typeof parent[key]) {
      parent[key] = val;
    ***REMOVED*** else if ('undefined' == typeof parent[key]) {
      parent[key] = val;
    ***REMOVED*** else {
      parent[key] = [parent[key], val];
    ***REMOVED***
    // array
  ***REMOVED*** else {
    var obj = parent[key] = parent[key] || [];
    if (']' == part) {
      if (isArray(obj)) {
        if ('' != val) obj.push(val);
      ***REMOVED*** else if ('object' == typeof obj) {
        obj[objectKeys(obj).length] = val;
      ***REMOVED*** else {
        obj = parent[key] = [parent[key], val];
      ***REMOVED***
      // prop
    ***REMOVED*** else if (~indexOf(part, ']')) {
      part = part.substr(0, part.length - 1);
      if (!isint.test(part) && isArray(obj)) obj = promote(parent, key);
      parse(parts, obj, part, val);
      // key
    ***REMOVED*** else {
      if (!isint.test(part) && isArray(obj)) obj = promote(parent, key);
      parse(parts, obj, part, val);
    ***REMOVED***
  ***REMOVED***
***REMOVED***

/**
 * Merge parent key/val pair.
 */

function merge(parent, key, val){
  if (~indexOf(key, ']')) {
    var parts = key.split('[')
      , len = parts.length
      , last = len - 1;
    parse(parts, parent, 'base', val);
    // optimize
  ***REMOVED*** else {
    if (!isint.test(key) && isArray(parent.base)) {
      var t = {***REMOVED***;
      for (var k in parent.base) t[k] = parent.base[k];
      parent.base = t;
    ***REMOVED***
    set(parent.base, key, val);
  ***REMOVED***

  return parent;
***REMOVED***

/**
 * Compact sparse arrays.
 */

function compact(obj) {
  if ('object' != typeof obj) return obj;

  if (isArray(obj)) {
    var ret = [];

    for (var i in obj) {
      if (hasOwnProperty.call(obj, i)) {
        ret.push(obj[i]);
      ***REMOVED***
    ***REMOVED***

    return ret;
  ***REMOVED***

  for (var key in obj) {
    obj[key] = compact(obj[key]);
  ***REMOVED***

  return obj;
***REMOVED***

/**
 * Parse the given obj.
 */

function parseObject(obj){
  var ret = { base: {***REMOVED*** ***REMOVED***;

  forEach(objectKeys(obj), function(name){
    merge(ret, name, obj[name]);
  ***REMOVED***);

  return compact(ret.base);
***REMOVED***

/**
 * Parse the given str.
 */

function parseString(str){
  var ret = reduce(String(str).split('&'), function(ret, pair){
    var eql = indexOf(pair, '=')
      , brace = lastBraceInKey(pair)
      , key = pair.substr(0, brace || eql)
      , val = pair.substr(brace || eql, pair.length)
      , val = val.substr(indexOf(val, '=') + 1, val.length);

    // ?foo
    if ('' == key) key = pair, val = '';
    if ('' == key) return ret;

    return merge(ret, decode(key), decode(val));
  ***REMOVED***, { base: {***REMOVED*** ***REMOVED***).base;

  return compact(ret);
***REMOVED***

/**
 * Parse the given query `str` or `obj`, returning an object.
 *
 * @param {String***REMOVED*** str | {Object***REMOVED*** obj
 * @return {Object***REMOVED***
 * @api public
 */

exports.parse = function(str){
  if (null == str || '' == str) return {***REMOVED***;
  return 'object' == typeof str
    ? parseObject(str)
    : parseString(str);
***REMOVED***;

/**
 * Turn the given `obj` into a query string
 *
 * @param {Object***REMOVED*** obj
 * @return {String***REMOVED***
 * @api public
 */

var stringify = exports.stringify = function(obj, prefix) {
  if (isArray(obj)) {
    return stringifyArray(obj, prefix);
  ***REMOVED*** else if ('[object Object]' == toString.call(obj)) {
    return stringifyObject(obj, prefix);
  ***REMOVED*** else if ('string' == typeof obj) {
    return stringifyString(obj, prefix);
  ***REMOVED*** else {
    return prefix + '=' + encodeURIComponent(String(obj));
  ***REMOVED***
***REMOVED***;

/**
 * Stringify the given `str`.
 *
 * @param {String***REMOVED*** str
 * @param {String***REMOVED*** prefix
 * @return {String***REMOVED***
 * @api private
 */

function stringifyString(str, prefix) {
  if (!prefix) throw new TypeError('stringify expects an object');
  return prefix + '=' + encodeURIComponent(str);
***REMOVED***

/**
 * Stringify the given `arr`.
 *
 * @param {Array***REMOVED*** arr
 * @param {String***REMOVED*** prefix
 * @return {String***REMOVED***
 * @api private
 */

function stringifyArray(arr, prefix) {
  var ret = [];
  if (!prefix) throw new TypeError('stringify expects an object');
  for (var i = 0; i < arr.length; i++) {
    ret.push(stringify(arr[i], prefix + '[' + i + ']'));
  ***REMOVED***
  return ret.join('&');
***REMOVED***

/**
 * Stringify the given `obj`.
 *
 * @param {Object***REMOVED*** obj
 * @param {String***REMOVED*** prefix
 * @return {String***REMOVED***
 * @api private
 */

function stringifyObject(obj, prefix) {
  var ret = []
    , keys = objectKeys(obj)
    , key;

  for (var i = 0, len = keys.length; i < len; ++i) {
    key = keys[i];
    if ('' == key) continue;
    if (null == obj[key]) {
      ret.push(encodeURIComponent(key) + '=');
    ***REMOVED*** else {
      ret.push(stringify(obj[key], prefix
        ? prefix + '[' + encodeURIComponent(key) + ']'
        : encodeURIComponent(key)));
    ***REMOVED***
  ***REMOVED***

  return ret.join('&');
***REMOVED***

/**
 * Set `obj`'s `key` to `val` respecting
 * the weird and wonderful syntax of a qs,
 * where "foo=bar&foo=baz" becomes an array.
 *
 * @param {Object***REMOVED*** obj
 * @param {String***REMOVED*** key
 * @param {String***REMOVED*** val
 * @api private
 */

function set(obj, key, val) {
  var v = obj[key];
  if (Object.getOwnPropertyDescriptor(Object.prototype, key)) return;
  if (undefined === v) {
    obj[key] = val;
  ***REMOVED*** else if (isArray(v)) {
    v.push(val);
  ***REMOVED*** else {
    obj[key] = [v, val];
  ***REMOVED***
***REMOVED***

/**
 * Locate last brace in `str` within the key.
 *
 * @param {String***REMOVED*** str
 * @return {Number***REMOVED***
 * @api private
 */

function lastBraceInKey(str) {
  var len = str.length
    , brace
    , c;
  for (var i = 0; i < len; ++i) {
    c = str[i];
    if (']' == c) brace = false;
    if ('[' == c) brace = true;
    if ('=' == c && !brace) return i;
  ***REMOVED***
***REMOVED***

/**
 * Decode `str`.
 *
 * @param {String***REMOVED*** str
 * @return {String***REMOVED***
 * @api private
 */

function decode(str) {
  try {
    return decodeURIComponent(str.replace(/\+/g, ' '));
  ***REMOVED*** catch (err) {
    return str;
  ***REMOVED***
***REMOVED***
