exports.lookup = function(obj, field) {
  if (!obj) { return null; ***REMOVED***
  var chain = field.split(']').join('').split('[');
  for (var i = 0, len = chain.length; i < len; i++) {
    var prop = obj[chain[i]];
    if (typeof(prop) === 'undefined') { return null; ***REMOVED***
    if (typeof(prop) !== 'object') { return prop; ***REMOVED***
    obj = prop;
  ***REMOVED***
  return null;
***REMOVED***;
