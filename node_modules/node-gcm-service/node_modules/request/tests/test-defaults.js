var server = require('./server')
  , assert = require('assert')
  , request = require('../index')
  ;

var s = server.createServer();

s.listen(s.port, function () {
  var counter = 0;
  s.on('/get', function (req, resp) {
    assert.equal(req.headers.foo, 'bar');
    assert.equal(req.method, 'GET')
    resp.writeHead(200, {'Content-Type': 'text/plain'***REMOVED***);
    resp.end('TESTING!');
  ***REMOVED***);

  // test get(string, function)
  request.defaults({headers:{foo:"bar"***REMOVED******REMOVED***)(s.url + '/get', function (e, r, b){
    if (e) throw e;
    assert.deepEqual("TESTING!", b);
    counter += 1;
  ***REMOVED***);

  s.on('/post', function (req, resp) {
    assert.equal(req.headers.foo, 'bar');
    assert.equal(req.headers['content-type'], null);
    assert.equal(req.method, 'POST')
    resp.writeHead(200, {'Content-Type': 'application/json'***REMOVED***);
    resp.end(JSON.stringify({foo:'bar'***REMOVED***));
  ***REMOVED***);

  // test post(string, object, function)
  request.defaults({headers:{foo:"bar"***REMOVED******REMOVED***).post(s.url + '/post', {json: true***REMOVED***, function (e, r, b){
    if (e) throw e;
    assert.deepEqual('bar', b.foo);
    counter += 1;
  ***REMOVED***);

  s.on('/patch', function (req, resp) {
    assert.equal(req.headers.foo, 'bar');
    assert.equal(req.headers['content-type'], null);
    assert.equal(req.method, 'PATCH')
    resp.writeHead(200, {'Content-Type': 'application/json'***REMOVED***);
    resp.end(JSON.stringify({foo:'bar'***REMOVED***));
  ***REMOVED***);

  // test post(string, object, function)
  request.defaults({headers:{foo:"bar"***REMOVED******REMOVED***).patch(s.url + '/patch', {json: true***REMOVED***, function (e, r, b){
    if (e) throw e;
    assert.deepEqual('bar', b.foo);
    counter += 1;
  ***REMOVED***);

  s.on('/post-body', function (req, resp) {
    assert.equal(req.headers.foo, 'bar');
    assert.equal(req.headers['content-type'], 'application/json');
    assert.equal(req.method, 'POST')
    resp.writeHead(200, {'Content-Type': 'application/json'***REMOVED***);
    resp.end(JSON.stringify({foo:'bar'***REMOVED***));
  ***REMOVED***);

  // test post(string, object, function) with body
  request.defaults({headers:{foo:"bar"***REMOVED******REMOVED***).post(s.url + '/post-body', {json: true, body:{bar:"baz"***REMOVED******REMOVED***, function (e, r, b){
    if (e) throw e;
    assert.deepEqual('bar', b.foo);
    counter += 1;
  ***REMOVED***);

  s.on('/del', function (req, resp) {
    assert.equal(req.headers.foo, 'bar');
    assert.equal(req.method, 'DELETE')
    resp.writeHead(200, {'Content-Type': 'application/json'***REMOVED***);
    resp.end(JSON.stringify({foo:'bar'***REMOVED***));
  ***REMOVED***);

  // test .del(string, function)
  request.defaults({headers:{foo:"bar"***REMOVED***, json:true***REMOVED***).del(s.url + '/del', function (e, r, b){
    if (e) throw e;
    assert.deepEqual('bar', b.foo);
    counter += 1;
  ***REMOVED***);

  s.on('/head', function (req, resp) {
    assert.equal(req.headers.foo, 'bar');
    assert.equal(req.method, 'HEAD')
    resp.writeHead(200, {'Content-Type': 'text/plain'***REMOVED***);
    resp.end();
  ***REMOVED***);

  // test head.(object, function)
  request.defaults({headers:{foo:"bar"***REMOVED******REMOVED***).head({uri: s.url + '/head'***REMOVED***, function (e, r, b){
    if (e) throw e;
    counter += 1;
  ***REMOVED***);

  s.on('/get_custom', function(req, resp) {
    assert.equal(req.headers.foo, 'bar');
    assert.equal(req.headers.x, 'y');
    resp.writeHead(200, {'Content-Type': 'text/plain'***REMOVED***);
    resp.end();
  ***REMOVED***);

  // test custom request handler function
  var defaultRequest = request.defaults({
    headers:{foo:"bar"***REMOVED***
    , body: 'TESTING!'
  ***REMOVED***, function(uri, options, callback) {
    var params = request.initParams(uri, options, callback);
    options = params.options;
    options.headers.x = 'y';

    return request(params.uri, params.options, params.callback);
  ***REMOVED***);

  var msg = 'defaults test failed. head request should throw earlier';
  assert.throws(function() {
    defaultRequest.head(s.url + '/get_custom', function(e, r, b) {
      throw new Error(msg);
    ***REMOVED***);
    counter+=1;
  ***REMOVED***, msg);

  defaultRequest.get(s.url + '/get_custom', function(e, r, b) {
    if(e) throw e;
    counter += 1;
    console.log(counter.toString() + " tests passed.");
    s.close();
  ***REMOVED***);
***REMOVED***)
