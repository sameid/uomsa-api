//===========================================================================

#ifndef BSON_H_
#define BSON_H_

//===========================================================================

#ifdef __arm__
#define USE_MISALIGNED_MEMORY_ACCESS 0
#else
#define USE_MISALIGNED_MEMORY_ACCESS 1
#endif

#include <node.h>
#include <node_object_wrap.h>
#include <v8.h>

using namespace v8;
using namespace node;

//===========================================================================

enum BsonType
{
	BSON_TYPE_NUMBER		= 1,
	BSON_TYPE_STRING		= 2,
	BSON_TYPE_OBJECT		= 3,
	BSON_TYPE_ARRAY			= 4,
	BSON_TYPE_BINARY		= 5,
	BSON_TYPE_UNDEFINED		= 6,
	BSON_TYPE_OID			= 7,
	BSON_TYPE_BOOLEAN		= 8,
	BSON_TYPE_DATE			= 9,
	BSON_TYPE_NULL			= 10,
	BSON_TYPE_REGEXP		= 11,
	BSON_TYPE_CODE			= 13,
	BSON_TYPE_SYMBOL		= 14,
	BSON_TYPE_CODE_W_SCOPE	= 15,
	BSON_TYPE_INT			= 16,
	BSON_TYPE_TIMESTAMP		= 17,
	BSON_TYPE_LONG			= 18,
	BSON_TYPE_MAX_KEY		= 0x7f,
	BSON_TYPE_MIN_KEY		= 0xff
***REMOVED***;

//===========================================================================

template<typename T> class BSONSerializer;

class BSON : public ObjectWrap {
public:    
	BSON();
	~BSON() {***REMOVED***

	static void Initialize(Handle<Object> target);
	static Handle<Value> BSONDeserializeStream(const Arguments &args);

	// JS based objects
	static Handle<Value> BSONSerialize(const Arguments &args);
	static Handle<Value> BSONDeserialize(const Arguments &args);

	// Calculate size of function
	static Handle<Value> CalculateObjectSize(const Arguments &args);
	static Handle<Value> SerializeWithBufferAndIndex(const Arguments &args);

	// Constructor used for creating new BSON objects from C++
	static Persistent<FunctionTemplate> constructor_template;

private:
	static Handle<Value> New(const Arguments &args);
	static Handle<Value> deserialize(BSON *bson, char *data, uint32_t dataLength, uint32_t startIndex, bool is_array_item);

	// BSON type instantiate functions
	Persistent<Function> longConstructor;
	Persistent<Function> objectIDConstructor;
	Persistent<Function> binaryConstructor;
	Persistent<Function> codeConstructor;
	Persistent<Function> dbrefConstructor;
	Persistent<Function> symbolConstructor;
	Persistent<Function> doubleConstructor;
	Persistent<Function> timestampConstructor;
	Persistent<Function> minKeyConstructor;
	Persistent<Function> maxKeyConstructor;

	// Equality Objects
	Persistent<String> longString;
	Persistent<String> objectIDString;
	Persistent<String> binaryString;
	Persistent<String> codeString;
	Persistent<String> dbrefString;
	Persistent<String> symbolString;
	Persistent<String> doubleString;
	Persistent<String> timestampString;
	Persistent<String> minKeyString;
	Persistent<String> maxKeyString;

	// Equality speed up comparison objects
	Persistent<String> _bsontypeString;
	Persistent<String> _longLowString;
	Persistent<String> _longHighString;
	Persistent<String> _objectIDidString;
	Persistent<String> _binaryPositionString;
	Persistent<String> _binarySubTypeString;
	Persistent<String> _binaryBufferString;
	Persistent<String> _doubleValueString;
	Persistent<String> _symbolValueString;

	Persistent<String> _dbRefRefString;
	Persistent<String> _dbRefIdRefString;
	Persistent<String> _dbRefDbRefString;
	Persistent<String> _dbRefNamespaceString;
	Persistent<String> _dbRefDbString;
	Persistent<String> _dbRefOidString;

	Persistent<String> _codeCodeString;
	Persistent<String> _codeScopeString;
	Persistent<String> _toBSONString;

	Local<Object> GetSerializeObject(const Handle<Value>& object);

	template<typename T> friend class BSONSerializer;
	friend class BSONDeserializer;
***REMOVED***;

//===========================================================================

class CountStream
{
public:
	CountStream() : count(0) { ***REMOVED***

	void	WriteByte(int value)									{ ++count; ***REMOVED***
	void	WriteByte(const Handle<Object>&, const Handle<String>&)	{ ++count; ***REMOVED***
	void	WriteBool(const Handle<Value>& value)					{ ++count; ***REMOVED***
	void	WriteInt32(int32_t value)								{ count += 4; ***REMOVED***
	void	WriteInt32(const Handle<Value>& value)					{ count += 4; ***REMOVED***
	void	WriteInt32(const Handle<Object>& object, const Handle<String>& key) { count += 4; ***REMOVED***
	void	WriteInt64(int64_t value)								{ count += 8; ***REMOVED***
	void	WriteInt64(const Handle<Value>& value)					{ count += 8; ***REMOVED***
	void	WriteDouble(double value)								{ count += 8; ***REMOVED***
	void	WriteDouble(const Handle<Value>& value)					{ count += 8; ***REMOVED***
	void	WriteDouble(const Handle<Object>&, const Handle<String>&) { count += 8; ***REMOVED***
	void	WriteUInt32String(uint32_t name)						{ char buffer[32]; count += sprintf(buffer, "%u", name) + 1; ***REMOVED***
	void	WriteLengthPrefixedString(const Local<String>& value)	{ count += value->Utf8Length()+5; ***REMOVED***
	void	WriteObjectId(const Handle<Object>& object, const Handle<String>& key)				{ count += 12; ***REMOVED***
	void	WriteString(const Local<String>& value)					{ count += value->Utf8Length() + 1; ***REMOVED***	// This returns the number of bytes exclusive of the NULL terminator
	void	WriteData(const char* data, size_t length)				{ count += length; ***REMOVED***

	void*	BeginWriteType()										{ ++count; return NULL; ***REMOVED***
	void	CommitType(void*, BsonType)								{ ***REMOVED***
	void*	BeginWriteSize()										{ count += 4; return NULL; ***REMOVED***
	void	CommitSize(void*)										{ ***REMOVED***

	size_t GetSerializeSize() const									{ return count; ***REMOVED***

	// Do nothing. CheckKey is implemented for DataStream
	void	CheckKey(const Local<String>&)							{ ***REMOVED***

private:
	size_t	count;
***REMOVED***;

class DataStream
{
public:
	DataStream(char* aDestinationBuffer) : destinationBuffer(aDestinationBuffer), p(aDestinationBuffer) { ***REMOVED***

	void	WriteByte(int value)									{ *p++ = value; ***REMOVED***
	void	WriteByte(const Handle<Object>& object, const Handle<String>& key)	{ *p++ = object->Get(key)->Int32Value(); ***REMOVED***
#if USE_MISALIGNED_MEMORY_ACCESS
	void	WriteInt32(int32_t value)								{ *reinterpret_cast<int32_t*>(p) = value; p += 4; ***REMOVED***
	void	WriteInt64(int64_t value)								{ *reinterpret_cast<int64_t*>(p) = value; p += 8; ***REMOVED***
	void	WriteDouble(double value)								{ *reinterpret_cast<double*>(p) = value; p += 8; ***REMOVED***
#else
	void	WriteInt32(int32_t value)								{ memcpy(p, &value, 4); p += 4; ***REMOVED***
	void	WriteInt64(int64_t value)								{ memcpy(p, &value, 8); p += 8; ***REMOVED***
	void	WriteDouble(double value)								{ memcpy(p, &value, 8); p += 8; ***REMOVED***
#endif
	void	WriteBool(const Handle<Value>& value)					{ WriteByte(value->BooleanValue() ? 1 : 0); ***REMOVED***
	void	WriteInt32(const Handle<Value>& value)					{ WriteInt32(value->Int32Value());		***REMOVED***
	void	WriteInt32(const Handle<Object>& object, const Handle<String>& key) { WriteInt32(object->Get(key)); ***REMOVED***
	void	WriteInt64(const Handle<Value>& value)					{ WriteInt64(value->IntegerValue());	***REMOVED***
	void	WriteDouble(const Handle<Value>& value)					{ WriteDouble(value->NumberValue());	***REMOVED***
	void	WriteDouble(const Handle<Object>& object, const Handle<String>& key) { WriteDouble(object->Get(key)); ***REMOVED***
	void	WriteUInt32String(uint32_t name)						{ p += sprintf(p, "%u", name) + 1;		***REMOVED***
	void	WriteLengthPrefixedString(const Local<String>& value)	{ WriteInt32(value->Utf8Length()+1); WriteString(value); ***REMOVED***
	void	WriteObjectId(const Handle<Object>& object, const Handle<String>& key);
	void	WriteString(const Local<String>& value)					{ p += value->WriteUtf8(p); ***REMOVED***		// This returns the number of bytes inclusive of the NULL terminator.
	void	WriteData(const char* data, size_t length)				{ memcpy(p, data, length); p += length; ***REMOVED***

	void*	BeginWriteType()										{ void* returnValue = p; p++; return returnValue; ***REMOVED***
	void	CommitType(void* beginPoint, BsonType value)			{ *reinterpret_cast<unsigned char*>(beginPoint) = value; ***REMOVED***
	void*	BeginWriteSize()										{ void* returnValue = p; p += 4; return returnValue; ***REMOVED***

#if USE_MISALIGNED_MEMORY_ACCESS
	void	CommitSize(void* beginPoint)							{ *reinterpret_cast<int32_t*>(beginPoint) = (int32_t) (p - (char*) beginPoint); ***REMOVED***
#else
	void	CommitSize(void* beginPoint)							{ int32_t value = (int32_t) (p - (char*) beginPoint); memcpy(beginPoint, &value, 4); ***REMOVED***
#endif

	size_t GetSerializeSize() const									{ return p - destinationBuffer; ***REMOVED***

	void	CheckKey(const Local<String>& keyName);

protected:
	char *const	destinationBuffer;		// base, never changes
	char*		p;						// cursor into buffer
***REMOVED***;

template<typename T> class BSONSerializer : public T
{
private:
	typedef T Inherited;

public:
	BSONSerializer(BSON* aBson, bool aCheckKeys, bool aSerializeFunctions) : Inherited(), checkKeys(aCheckKeys), serializeFunctions(aSerializeFunctions), bson(aBson) { ***REMOVED***
	BSONSerializer(BSON* aBson, bool aCheckKeys, bool aSerializeFunctions, char* parentParam) : Inherited(parentParam), checkKeys(aCheckKeys), serializeFunctions(aSerializeFunctions), bson(aBson) { ***REMOVED***

	void SerializeDocument(const Handle<Value>& value);
	void SerializeArray(const Handle<Value>& value);
	void SerializeValue(void* typeLocation, const Handle<Value>& value);

private:
	bool		checkKeys;
	bool		serializeFunctions;
	BSON*		bson;
***REMOVED***;

//===========================================================================

class BSONDeserializer
{
public:
	BSONDeserializer(BSON* aBson, char* data, size_t length);
	BSONDeserializer(BSONDeserializer& parentSerializer, size_t length);

	Handle<Value> DeserializeDocument(bool promoteLongs);

	bool			HasMoreData() const { return p < pEnd; ***REMOVED***
	Local<String>	ReadCString();
	uint32_t		ReadIntegerString();
	int32_t			ReadRegexOptions();
	Local<String>	ReadString();
	Local<String>	ReadObjectId();

	unsigned char	ReadByte()			{ return *reinterpret_cast<unsigned char*>(p++); ***REMOVED***
#if USE_MISALIGNED_MEMORY_ACCESS
	int32_t			ReadInt32()			{ int32_t returnValue = *reinterpret_cast<int32_t*>(p); p += 4; return returnValue; ***REMOVED***
	uint32_t		ReadUInt32()		{ uint32_t returnValue = *reinterpret_cast<uint32_t*>(p); p += 4; return returnValue; ***REMOVED***
	int64_t			ReadInt64()			{ int64_t returnValue = *reinterpret_cast<int64_t*>(p); p += 8; return returnValue; ***REMOVED***
	double			ReadDouble()		{ double returnValue = *reinterpret_cast<double*>(p); p += 8; return returnValue; ***REMOVED***
#else
	int32_t			ReadInt32()			{ int32_t returnValue; memcpy(&returnValue, p, 4); p += 4; return returnValue; ***REMOVED***
	uint32_t		ReadUInt32()		{ uint32_t returnValue; memcpy(&returnValue, p, 4); p += 4; return returnValue; ***REMOVED***
	int64_t			ReadInt64()			{ int64_t returnValue; memcpy(&returnValue, p, 8); p += 8; return returnValue; ***REMOVED***
	double			ReadDouble()		{ double returnValue; memcpy(&returnValue, p, 8); p += 8; return returnValue; ***REMOVED***
#endif

	size_t			GetSerializeSize() const { return p - pStart; ***REMOVED***

private:
	Handle<Value> DeserializeArray(bool promoteLongs);
	Handle<Value> DeserializeValue(BsonType type, bool promoteLongs);
	Handle<Value> DeserializeDocumentInternal(bool promoteLongs);
	Handle<Value> DeserializeArrayInternal(bool promoteLongs);

	BSON*		bson;
	char* const pStart;
	char*		p;
	char* const	pEnd;
***REMOVED***;

//===========================================================================

#endif  // BSON_H_

//===========================================================================
