
/*!
 * Module dependencies
 */

var utils = require('./utils')

/*!
 * Prepare a set of path options for query population.
 *
 * @param {Query***REMOVED*** query
 * @param {Object***REMOVED*** options
 * @return {Array***REMOVED***
 */

exports.preparePopulationOptions = function preparePopulationOptions (query, options) {
  var pop = utils.object.vals(query.options.populate);

  // lean options should trickle through all queries
  if (options.lean) pop.forEach(makeLean);

  return pop;
***REMOVED***

/*!
 * Set each path query option to lean
 *
 * @param {Object***REMOVED*** option
 */

function makeLean (option) {
  option.options || (option.options = {***REMOVED***);
  option.options.lean = true;
***REMOVED***

