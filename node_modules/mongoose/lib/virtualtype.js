
/**
 * VirtualType constructor
 *
 * This is what mongoose uses to define virtual attributes via `Schema.prototype.virtual`.
 *
 * ####Example:
 *
 *     var fullname = schema.virtual('fullname');
 *     fullname instanceof mongoose.VirtualType // true
 *
 * @parma {Object***REMOVED*** options
 * @api public
 */

function VirtualType (options, name) {
  this.path = name;
  this.getters = [];
  this.setters = [];
  this.options = options || {***REMOVED***;
***REMOVED***

/**
 * Defines a getter.
 *
 * ####Example:
 *
 *     var virtual = schema.virtual('fullname');
 *     virtual.get(function () {
 *       return this.name.first + ' ' + this.name.last;
 *     ***REMOVED***);
 *
 * @param {Function***REMOVED*** fn
 * @return {VirtualType***REMOVED*** this
 * @api public
 */

VirtualType.prototype.get = function (fn) {
  this.getters.push(fn);
  return this;
***REMOVED***;

/**
 * Defines a setter.
 *
 * ####Example:
 *
 *     var virtual = schema.virtual('fullname');
 *     virtual.set(function (v) {
 *       var parts = v.split(' ');
 *       this.name.first = parts[0];
 *       this.name.last = parts[1];
 *     ***REMOVED***);
 *
 * @param {Function***REMOVED*** fn
 * @return {VirtualType***REMOVED*** this
 * @api public
 */

VirtualType.prototype.set = function (fn) {
  this.setters.push(fn);
  return this;
***REMOVED***;

/**
 * Applies getters to `value` using optional `scope`.
 *
 * @param {Object***REMOVED*** value
 * @param {Object***REMOVED*** scope
 * @return {any***REMOVED*** the value after applying all getters
 * @api public
 */

VirtualType.prototype.applyGetters = function (value, scope) {
  var v = value;
  for (var l = this.getters.length - 1; l >= 0; l--) {
    v = this.getters[l].call(scope, v, this);
  ***REMOVED***
  return v;
***REMOVED***;

/**
 * Applies setters to `value` using optional `scope`.
 *
 * @param {Object***REMOVED*** value
 * @param {Object***REMOVED*** scope
 * @return {any***REMOVED*** the value after applying all setters
 * @api public
 */

VirtualType.prototype.applySetters = function (value, scope) {
  var v = value;
  for (var l = this.setters.length - 1; l >= 0; l--) {
    v = this.setters[l].call(scope, v, this);
  ***REMOVED***
  return v;
***REMOVED***;

/*!
 * exports
 */

module.exports = VirtualType;
